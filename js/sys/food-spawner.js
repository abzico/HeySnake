import Food from '../object/food'

export default class FoodSpawner {
  /**
   * Spawn a new food in empty tile according to specifications from tilemap
   * @param {Snake} snake Snake
   * @param {Tilemap} tilemap Tilemap
   */
  spawnFood(snake, tilemap) {
    
  }

  
}